{
	"name": "local-agent",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"workspaces": ["packages/*"],
	"scripts": {
		"lint": "biome check .",
		"lint:fix": "biome check --write .",
		"format": "biome format --write .",
		"test": "npm test --workspaces --if-present"
	},
	"devDependencies": {
		"@biomejs/biome": "^1.9.0"
	}
}
